/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using webpack (not Turbopack) for production builds
  // Turbopack is only used locally via "next dev --turbopack"
};

export default nextConfig;
